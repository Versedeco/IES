# 代码修正说明

## 修改日期
2026-01-21

## 主要修正内容

### 1. 修正了 `lumens_to_radiant_watts` 方法的核心计算逻辑

**原有错误代码:**
```python
actual_visible_fraction = specs["visible_fraction"] * temp_factor
radiant_watts = lumens / (max_luminous_efficacy * actual_visible_fraction)
```

**问题分析:**
- 混淆了"光视效能"和"可见光比例"两个不同的物理概念
- `visible_fraction` 是辐射能中可见光的比例，不应该直接用于流明到辐射瓦特的转换
- 正确的关系是: `流明 = 可见光辐射瓦特 × 光视效能`

**修正后代码:**
```python
adjusted_efficacy = specs["typical_efficacy"] * temp_factor
relative_efficacy = adjusted_efficacy / max_luminous_efficacy
radiant_watts = lumens / (max_luminous_efficacy * relative_efficacy)
```

**物理原理:**
- 光视效能 = 流明 / 可见光辐射瓦特
- 因此: 可见光辐射瓦特 = 流明 / 光视效能
- 使用相对光视效能（相对于理论最大值683 lm/W的比例）来计算

### 2. 更新了部分光源参数

**LED灯:**
- `radiant_efficiency`: 0.25 → 0.35 (更符合现代LED的实际效率)

**高端LED灯:**
- `radiant_efficiency`: 0.35 → 0.45 (高端LED效率更高)

**白炽灯:**
- `visible_fraction`: 0.03 → 0.08 (更符合实际的5-10%可见光比例)

## 物理概念说明

### 三个关键参数的区别:

1. **radiant_efficiency (辐射效率)**
   - 电功率 → 辐射功率的转换效率
   - 包括所有波长的辐射（可见光、红外、紫外等）
   - 例: LED约35%，白炽灯约95%

2. **visible_fraction (可见光比例)**
   - 辐射功率中可见光部分的比例
   - 例: LED约30%，白炽灯约8%（大部分是红外热辐射）

3. **luminous_efficacy (光视效能)**
   - 可见光辐射功率 → 光通量的转换效率
   - 单位: lm/W
   - 理论最大值: 683 lm/W (555nm绿光)
   - 实际值取决于光谱分布

### 转换关系:

```
电功率 (W)
  ↓ × radiant_efficiency
总辐射功率 (W)
  ↓ × visible_fraction
可见光辐射功率 (W)
  ↓ × luminous_efficacy
光通量 (lm)
```

## 使用建议

### 方法1: 从流明计算（推荐）
```python
converter = LampSpecsToRadiantConverter()
radiant_watts, method = converter.calculate_radiant_watts(
    lumens=1000,           # 厂商提供的光通量
    source_type="led",     # 光源类型
    color_temp=4000        # 色温
)
# 结果: 可见光辐射功率，适合用于Blender的Radiant Watts设置
```

### 方法2: 从电功率计算
```python
radiant_watts, method = converter.calculate_radiant_watts(
    electrical_watts=60,      # 灯具功率
    source_type="incandescent",
    color_temp=2700
)
# 结果: 总辐射功率（含红外等），包含所有波长
```

## 注意事项

1. **Blender的Radiant Watts含义**
   - 如果Blender期望的是"可见光辐射功率"，使用方法1（从流明计算）
   - 如果Blender期望的是"总辐射功率"，使用方法2（从电功率计算）

2. **两种方法的差异**
   - 从流明计算: 只计算可见光部分
   - 从电功率计算: 包含所有辐射（可见光+红外+紫外）
   - 对于白炽灯，两者差异很大（只有5-10%是可见光）

3. **实际应用建议**
   - 优先使用厂商提供的流明值
   - 流明值更准确反映人眼感知的亮度
   - 适合照明计算和渲染

## 测试验证

运行测试脚本验证修正效果:
```bash
python test_conversion.py
```

或使用命令行工具:
```bash
python ProductsParametersToRadiantWatts.py --lumens 1000 --type led --temp 4000
python ProductsParametersToRadiantWatts.py --watts 60 --type incandescent --temp 2700
```

## 参考资料

- 光视效能理论最大值: 683 lm/W @ 555nm
- LED典型光效: 80-150 lm/W
- 白炽灯典型光效: 10-20 lm/W
- 白炽灯辐射效率: 95% (但只有5-10%是可见光)
