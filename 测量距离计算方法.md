# 测量距离选择的物理依据与动态计算

## 📋 问题描述

**固定1米测量距离存在问题**：
- ❌ 对于大型灯具（如体育场照明），1米远远不够
- ❌ 对于小型灯具（如手电筒），1米可能过大
- ✅ 需要基于灯具实际尺寸动态计算

---

## 🔬 物理原理：远场条件

### 1. 为什么需要远场测量？

```
近场问题（d 太小）：
┌─────────────┐
│    灯具     │
│   ┌───┐    │  ← 灯具 0.5m × 0.5m
│   │ L │    │
│   └───┘    │
└─────────────┘
      ↓ 0.5m（测量距离太近！）
      ●  测量点

问题：
- 灯具不同部分对测量点的角度差异大
- 左边光源：45° 入射
- 右边光源：-45° 入射
- 无法视为点光源
- 测量结果不准确

远场解决（d 足够大）：
┌─────────────┐
│    灯具     │
│   ┌───┐    │  ← 灯具 0.5m × 0.5m
│   │ L │    │
│   └───┘    │
└─────────────┘
      ↓ 2.5m（5倍灯具尺寸）
      
      
      ●  测量点

优势：
- 所有光源点对测量点的角度近似相同
- 可以视为点光源
- 符合IES光强定义
- 测量准确
```

### 2. 数学判据

```python
# 远场条件判据
角度误差 = arctan(D_max / (2 × d))

要求角度误差 < 2°：
arctan(D_max / (2 × d)) < 2°
D_max / (2 × d) < tan(2°) ≈ 0.035
d > D_max / (2 × 0.035) ≈ 14.3 × D_max

工程简化（保守估计）：
d ≥ 5 × D_max  ← IES标准采用此值

说明：
- 5倍是工程实践中的平衡点
- 既保证精度，又不至于距离过大
- 角度误差约 5.7°，可接受范围
```

---

## 📐 IES标准要求

### IESNA LM-79 标准摘要

```
测量距离要求：

1. 小型灯具（D_max < 0.2m）：
   - 最小距离：1.0 米
   - 或 5 × D_max（取较大值）

2. 中型灯具（0.2m ≤ D_max ≤ 1.0m）：
   - 最小距离：5 × D_max

3. 大型灯具（D_max > 1.0m）：
   - 最小距离：5 × D_max
   - 最大距离：通常不超过20米（实验室限制）

特殊情况：
- 投光灯/探照灯：d ≥ 10 × D_max（更严格）
- 街道照明：d ≥ 3 × D_max（可适当放宽）
```

---

## 💻 动态计算实现

### 方法1：基于包围盒（推荐）

```python
import bpy
from mathutils import Vector
import numpy as np

def calculate_luminaire_bounding_sphere(luminaire_object):
    """
    计算灯具的最小外接球半径
    
    Args:
        luminaire_object: 灯具对象（包含所有子对象）
    
    Returns:
        center: 外接球中心（世界坐标）
        radius: 外接球半径（米）
    """
    # 收集所有相关对象
    objects = [luminaire_object]
    if luminaire_object.children:
        objects.extend(luminaire_object.children_recursive)
    
    # 收集所有顶点的世界坐标
    all_vertices = []
    for obj in objects:
        if obj.type == 'MESH':
            for vertex in obj.data.vertices:
                world_pos = obj.matrix_world @ vertex.co
                all_vertices.append(world_pos)
        elif obj.type == 'LIGHT':
            # 灯光对象使用其位置
            all_vertices.append(obj.matrix_world.translation)
    
    if not all_vertices:
        raise ValueError("灯具没有可用的几何体")
    
    # 转换为numpy数组
    vertices_array = np.array([[v.x, v.y, v.z] for v in all_vertices])
    
    # 计算中心（所有顶点的平均）
    center = vertices_array.mean(axis=0)
    center_vector = Vector(center)
    
    # 计算半径（最远顶点的距离）
    distances = np.linalg.norm(vertices_array - center, axis=1)
    radius = distances.max()
    
    return center_vector, radius

def calculate_measurement_distance(luminaire_object, 
                                   min_absolute=1.0, 
                                   multiplier=5.0,
                                   max_absolute=20.0):
    """
    计算IES测量的最佳距离
    
    Args:
        luminaire_object: 灯具对象
        min_absolute: 绝对最小距离（米），默认1.0m
        multiplier: 尺寸倍数，默认5.0（IES标准）
        max_absolute: 绝对最大距离（米），默认20.0m（实验室限制）
    
    Returns:
        measurement_distance: 推荐测量距离（米）
        luminaire_radius: 灯具外接球半径（米）
        info: 计算信息字典
    """
    # 计算灯具外接球
    center, luminaire_radius = calculate_luminaire_bounding_sphere(luminaire_object)
    
    # 基于尺寸的推荐距离
    size_based_distance = luminaire_radius * 2.0 * multiplier  # 直径 × 倍数
    
    # 应用约束
    measurement_distance = max(min_absolute, size_based_distance)
    measurement_distance = min(max_absolute, measurement_distance)
    
    # 验证远场条件
    angle_error_deg = np.degrees(np.arctan(luminaire_radius / measurement_distance))
    
    # 编译信息
    info = {
        'luminaire_center': center,
        'luminaire_radius': luminaire_radius,
        'luminaire_diameter': luminaire_radius * 2.0,
        'size_based_distance': size_based_distance,
        'final_distance': measurement_distance,
        'distance_to_radius_ratio': measurement_distance / (luminaire_radius * 2.0),
        'angle_error_deg': angle_error_deg,
        'meets_far_field': angle_error_deg < 10.0,  # 10°是宽松标准
        'meets_ies_standard': measurement_distance >= luminaire_radius * 2.0 * 5.0,
        'limited_by': 'size' if size_based_distance > min_absolute else 'minimum',
    }
    
    # 如果受最大距离限制
    if size_based_distance > max_absolute:
        info['limited_by'] = 'maximum'
        info['warning'] = f"灯具尺寸过大，测量距离受实验室限制在{max_absolute}m"
    
    return measurement_distance, luminaire_radius, info

# 使用示例
def example_usage():
    """使用示例"""
    # 假设已选择灯具
    luminaire = bpy.context.active_object
    
    # 计算测量距离
    distance, radius, info = calculate_measurement_distance(luminaire)
    
    # 打印报告
    print("\n" + "="*70)
    print("  📏 IES测量距离计算报告")
    print("="*70)
    print(f"灯具名称:        {luminaire.name}")
    print(f"外接球中心:      ({info['luminaire_center'].x:.3f}, "
          f"{info['luminaire_center'].y:.3f}, "
          f"{info['luminaire_center'].z:.3f}) m")
    print(f"外接球半径:      {radius:.3f} m")
    print(f"灯具最大直径:    {info['luminaire_diameter']:.3f} m")
    print("-"*70)
    print(f"基于尺寸的建议:  {info['size_based_distance']:.3f} m")
    print(f"最终测量距离:    {distance:.3f} m")
    print(f"距离/直径比:     {info['distance_to_radius_ratio']:.1f}:1")
    print("-"*70)
    print(f"角度误差:        {info['angle_error_deg']:.2f}°")
    print(f"满足远场条件:    {'✅ 是' if info['meets_far_field'] else '❌ 否'}")
    print(f"符合IES标准:     {'✅ 是' if info['meets_ies_standard'] else '⚠️  接近'}")
    print(f"限制因素:        {info['limited_by']}")
    
    if 'warning' in info:
        print(f"⚠️  警告: {info['warning']}")
    
    print("="*70 + "\n")
    
    return distance, info
```

### 方法2：基于包围盒（更简单）

```python
def calculate_measurement_distance_simple(luminaire_object):
    """
    简化版：基于Blender包围盒
    
    Args:
        luminaire_object: 灯具对象
    
    Returns:
        measurement_distance: 推荐测量距离
    """
    # 获取包围盒尺寸
    dimensions = luminaire_object.dimensions
    max_dimension = max(dimensions.x, dimensions.y, dimensions.z)
    
    # 计算外接球半径（包围盒的对角线的一半）
    diagonal = (dimensions.x**2 + dimensions.y**2 + dimensions.z**2)**0.5
    radius = diagonal / 2.0
    
    # IES标准：5倍直径，但不小于1米
    measurement_distance = max(1.0, radius * 2.0 * 5.0)
    
    print(f"📏 灯具尺寸: {dimensions.x:.3f} × {dimensions.y:.3f} × {dimensions.z:.3f} m")
    print(f"📏 最大尺寸: {max_dimension:.3f} m")
    print(f"📏 外接球半径: {radius:.3f} m")
    print(f"📏 推荐测量距离: {measurement_distance:.3f} m")
    
    return measurement_distance
```

---

## 🎯 不同尺寸灯具的处理策略

### 案例1：小型灯具（如LED灯珠）

```python
# LED灯珠：5mm × 5mm × 2mm
D_max = 0.005 m

# 按5倍计算
d = 5 × 0.005 = 0.025 m  ← 太小！

# 应用最小值限制
d = max(1.0, 0.025) = 1.0 m  ✅

结论：小型灯具统一使用1米测量距离
```

### 案例2：中型灯具（如筒灯）

```python
# 筒灯：直径150mm，高度100mm
外接球半径 ≈ 0.09 m
直径 = 0.18 m

# 按5倍计算
d = 5 × 0.18 = 0.9 m

# 应用最小值限制
d = max(1.0, 0.9) = 1.0 m  ✅

结论：刚好达到1米标准
```

### 案例3：大型灯具（如投光灯）

```python
# 体育场投光灯：1.2m × 0.8m × 0.6m
外接球半径 ≈ 0.78 m
直径 = 1.56 m

# 按5倍计算
d = 5 × 1.56 = 7.8 m  ✅

# 不受1米限制
d = max(1.0, 7.8) = 7.8 m

结论：使用7.8米测量距离
```

### 案例4：超大型灯具（如探照灯）

```python
# 军用探照灯：直径2.5m
外接球半径 = 1.25 m
直径 = 2.5 m

# 按5倍计算
d = 5 × 2.5 = 12.5 m  ✅

# 可能受实验室空间限制
d = min(12.5, 20.0) = 12.5 m

# 如果实验室只有10m高
d = 10.0 m
# 此时会有警告：未达到理想远场条件

结论：使用12.5米（如有空间），否则尽可能大
```

---

## 📊 测量距离选择流程图

```
开始
  ↓
计算灯具外接球半径 R
  ↓
计算理想测量距离: d_ideal = 5 × (2R) = 10R
  ↓
  ├─→ d_ideal < 1.0m → 使用 d = 1.0m（最小值）
  │
  ├─→ 1.0m ≤ d_ideal ≤ 20m → 使用 d = d_ideal（理想值）
  │
  └─→ d_ideal > 20m → 使用 d = 20m（最大值，发出警告）
  ↓
验证远场条件
  ├─→ 角度误差 < 10° → ✅ 通过
  └─→ 角度误差 ≥ 10° → ⚠️ 警告（但继续）
  ↓
创建测量球体（半径 = d）
  ↓
结束
```

---

## 🔧 完整代码整合

```python
# 在 EXRToIESConverter 类中更新

class EXRToIESConverter:
    def __init__(self, measurement_distance=None, resolution='high',
                 auto_calculate_distance=True):
        """
        初始化转换器
        
        Args:
            measurement_distance: 手动指定测量距离（米）
                                 如果为None且auto_calculate_distance=True，
                                 则自动计算
            resolution: EXR分辨率
            auto_calculate_distance: 是否自动计算测量距离
        """
        self.distance = measurement_distance
        self.auto_calculate_distance = auto_calculate_distance
        self.resolution = resolution
        
        # ... 其他初始化代码 ...
    
    def setup_measurement_distance(self, luminaire_object):
        """
        设置或计算测量距离
        
        Args:
            luminaire_object: 灯具对象
        
        Returns:
            distance: 最终测量距离
        """
        if self.distance is not None:
            # 手动指定的距离，验证是否合理
            _, radius, info = calculate_measurement_distance(
                luminaire_object, 
                multiplier=5.0
            )
            
            recommended = info['final_distance']
            
            if self.distance < recommended * 0.5:
                print(f"⚠️  警告：手动指定的距离 {self.distance:.2f}m "
                      f"小于推荐值 {recommended:.2f}m")
                print(f"   可能不满足远场条件，结果可能不准确")
            
            return self.distance
        
        elif self.auto_calculate_distance:
            # 自动计算
            distance, radius, info = calculate_measurement_distance(
                luminaire_object,
                min_absolute=1.0,
                multiplier=5.0,
                max_absolute=20.0
            )
            
            self.distance = distance
            
            # 打印详细报告
            print("\n" + "="*70)
            print("  📏 自动计算测量距离")
            print("="*70)
            print(f"灯具外接球半径:  {radius:.3f} m")
            print(f"推荐测量距离:    {distance:.3f} m")
            print(f"距离/直径比:     {info['distance_to_radius_ratio']:.1f}:1")
            print(f"角度误差:        {info['angle_error_deg']:.2f}°")
            print(f"符合IES标准:     {'✅' if info['meets_ies_standard'] else '⚠️'}")
            print("="*70 + "\n")
            
            return distance
        
        else:
            raise ValueError("必须指定measurement_distance或启用auto_calculate_distance")
    
    def create_measurement_sphere(self, luminaire_object, center=None):
        """
        创建测量球体（更新版）
        
        Args:
            luminaire_object: 灯具对象
            center: 球体中心，如果为None则使用灯具中心
        
        Returns:
            sphere: 球体对象
        """
        # 设置测量距离
        distance = self.setup_measurement_distance(luminaire_object)
        
        # 确定球心位置
        if center is None:
            center_vec, _ = calculate_luminaire_bounding_sphere(luminaire_object)
            center = (center_vec.x, center_vec.y, center_vec.z)
        
        # 创建UV球体
        bpy.ops.mesh.primitive_uv_sphere_add(
            segments=72,
            ring_count=36,
            radius=distance,
            location=center
        )
        
        sphere = bpy.context.active_object
        sphere.name = "IES_Measurement_Sphere"
        
        # 翻转法线
        bpy.context.view_layer.objects.active = sphere
        bpy.ops.object.mode_set(mode='EDIT')
        bpy.ops.mesh.select_all(action='SELECT')
        bpy.ops.mesh.flip_normals()
        bpy.ops.object.mode_set(mode='OBJECT')
        
        print(f"✅ 测量球体创建完成")
        print(f"   中心: ({center[0]:.3f}, {center[1]:.3f}, {center[2]:.3f}) m")
        print(f"   半径: {distance:.3f} m")
        
        return sphere
```

---

## 📋 使用建议总结

### 推荐做法

```python
# 方案1：完全自动（推荐）
converter = EXRToIESConverter(
    measurement_distance=None,  # 不指定
    auto_calculate_distance=True,  # 自动计算
    resolution='high'
)

# 方案2：手动指定（用于特殊情况）
converter = EXRToIESConverter(
    measurement_distance=5.0,  # 手动指定5米
    auto_calculate_distance=False,
    resolution='high'
)

# 方案3：计算后验证
distance, radius, info = calculate_measurement_distance(luminaire)
print(f"推荐距离: {distance}m")

# 根据需要调整
adjusted_distance = distance * 1.2  # 增加20%安全裕度

converter = EXRToIESConverter(
    measurement_distance=adjusted_distance,
    resolution='high'
)
```

### 特殊情况处理

```python
# 1. 空间受限（实验室天花板只有3米高）
converter = EXRToIESConverter(
    measurement_distance=2.5,  # 最大可用空间
    auto_calculate_distance=False
)
# 系统会警告但继续执行

# 2. 超高精度要求（科研级）
distance, _, _ = calculate_measurement_distance(
    luminaire,
    multiplier=10.0  # 使用10倍而非5倍
)
converter = EXRToIESConverter(measurement_distance=distance)

# 3. 快速预览（降低精度要求）
distance, _, _ = calculate_measurement_distance(
    luminaire,
    multiplier=3.0,  # 使用3倍（降低精度）
    min_absolute=0.5  # 小型灯具用0.5m
)
converter = EXRToIESConverter(measurement_distance=distance)
```

---

## ✅ 总结

### 关键要点

1. **动态计算原则**：
   - ✅ 测量距离应基于灯具实际尺寸
   - ✅ 使用 d = 5 × D_max 作为标准
   - ✅ 最小1米，最大20米（实际限制）

2. **物理依据**：
   - 远场条件：d >> D_max
   - IES标准：d ≥ 5 × D_max
   - 角度误差 < 10°可接受

3. **实现方法**：
   - 自动计算外接球半径
   - 应用倍数和约束
   - 提供验证和警告

4. **灵活性**：
   - 支持自动计算
   - 支持手动指定
   - 支持验证和调整

### 代码要点

```python
# 核心公式
d = max(1.0, min(20.0, 5.0 × luminaire_diameter))

# 验证
angle_error = arctan(radius / distance)
is_valid = angle_error < 10°
```

---

**感谢你的细心思考！这个问题非常重要，确保了方案的完整性和科学性。**

---

**文档版本**: 1.0  
**创建日期**: 2026-01-21  
**关联文档**: EXR转IES技术方案.md
