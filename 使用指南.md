# EXR to IES Converter - 使用指南

## 📋 概述

完整的Blender灯具光照分布测量和IES文件生成工具。

### 核心功能

✅ 自动计算测量距离（基于灯具尺寸）  
✅ 创建和配置测量球体  
✅ 烘焙光照分布到高动态范围EXR  
✅ 从EXR转换为标准IES格式  
✅ 支持完整的灯具参数描述  
✅ 批处理生成多个亮度版本  

---

## 🚀 快速开始

### 1. 准备工作

**在Blender中**：
1. 创建或导入灯具模型
2. 设置灯具的反射器、灯罩等组件
3. 添加光源（Point Light / Area Light）
4. 确保灯具完整（包含所有遮挡和反射组件）

**文件准备**：
- 确保 `ProductsParametersToRadiantWatts.py` 在同一目录
- 确保 `exr_to_ies_converter.py` 可访问

### 2. 最简使用

```python
import bpy
from exr_to_ies_converter import EXRToIESConverter, LuminaireParameters

# 选择灯具
luminaire = bpy.context.active_object

# 定义参数
params = LuminaireParameters(
    name="My Light",
    lumens=1000.0,
    source_type="led",
    color_temp=4000
)

# 创建转换器
converter = EXRToIESConverter()

# 执行转换
converter.process_full_workflow(
    luminaire_object=luminaire,
    luminaire_params=params,
    output_exr_path="output/my_light.exr",
    output_ies_path="output/my_light.ies"
)
```

---

## 📐 灯具参数详解

### LuminaireParameters 类

用于完整描述灯具的所有必要信息。

#### 基本信息参数

```python
params = LuminaireParameters(
    # 基本标识
    name="LED Downlight 1000lm",              # 灯具名称
    manufacturer="Custom Lighting Co.",        # 制造商
    catalog_number="DL-1000-4000K",           # 产品编号
    description="10W LED Downlight",          # 描述
)
```

#### 光学参数（必需）⭐⭐⭐⭐⭐

```python
params = LuminaireParameters(
    # 光学特性
    lumens=1000.0,              # ⭐ 总流明（最重要！）
    source_type="led",          # ⭐ 光源类型
    color_temp=4000,            # ⭐ 色温（K）
    cri=80,                     # 显色指数
)
```

**光源类型选项**：
- `"led"` - 普通LED（80-150 lm/W）
- `"led_high_end"` - 高端LED（120-200 lm/W）
- `"incandescent"` - 白炽灯（10-20 lm/W）
- `"halogen"` - 卤素灯（15-25 lm/W）
- `"fluorescent"` - 荧光灯/节能灯（50-80 lm/W）

#### 物理尺寸参数（推荐）⭐⭐⭐⭐

```python
params = LuminaireParameters(
    # 灯具尺寸（米）
    width=0.15,                 # 宽度
    length=0.15,                # 长度
    height=0.10,                # 高度
)
```

**作用**：
1. 用于计算测量距离（自动模式）
2. 写入IES文件头部
3. CG软件可读取用于正确缩放

#### 配光特性参数（可选）⭐⭐⭐

```python
params = LuminaireParameters(
    # 配光特性
    beam_angle=60,              # 光束角（度）
    field_angle=80,             # 截止角（度）
    photometric_type=1,         # 光度类型
)
```

**光度类型**：
- `1` - C平面（最常见，绕垂直轴对称）
- `2` - B平面（用于道路照明）
- `3` - A平面（用于泛光照明）

#### 电功率参数（可选，用于验证）⭐⭐

```python
params = LuminaireParameters(
    electrical_watts=10.0,      # 电功率（W）
)
```

**用途**：如果提供，可用于验证流明值是否合理

---

## ⚙️ 转换器配置

### EXRToIESConverter 参数

```python
converter = EXRToIESConverter(
    resolution='high',              # EXR分辨率
    sphere_density='standard',      # 球体网格密度
    cycles_samples=256,             # Cycles采样数
    auto_calculate_distance=True,   # 自动计算测量距离
    measurement_distance=None       # 或手动指定距离
)
```

### 配置选项详解

#### 1. EXR分辨率 (`resolution`)

| 选项 | 分辨率 | 像素数 | 用途 | 文件大小 |
|-----|--------|--------|------|---------|
| `'low'` | 1024×512 | 0.5M | 快速预览 | ~30MB |
| `'medium'` | 2048×1024 | 2.1M | 一般质量 | ~80MB |
| **`'high'`** | **4096×2048** | **8.4M** | **标准（推荐）** | **~130MB** |
| `'ultra'` | 8192×4096 | 33.5M | 极高精度 | ~450MB |

**推荐**：90%情况下使用 `'high'`

#### 2. 球体网格密度 (`sphere_density`)

| 选项 | 网格配置 | 面数 | 角分辨率 | 用途 |
|-----|---------|------|---------|------|
| `'preview'` | 36×18 | 1,296 | 10° | 快速测试 |
| **`'standard'`** | **72×36** | **2,592** | **5°** | **标准（推荐）** |
| `'fine'` | 144×72 | 10,368 | 2.5° | 高精度 |

**推荐**：使用 `'standard'`，除非：
- 极窄光束（<10°）→ 使用 `'fine'`
- 复杂配光（汽车灯）→ 使用 `'fine'`

#### 3. Cycles采样数 (`cycles_samples`)

| 采样数 | 质量 | 烘焙时间 | 用途 |
|-------|------|---------|------|
| 64 | 快速，有噪点 | 1-2分钟 | 预览 |
| 128 | 一般，轻微噪点 | 2-3分钟 | 测试 |
| **256** | **标准，噪点很少** | **3-5分钟** | **推荐** |
| 512 | 高质量，无噪点 | 6-10分钟 | 精品 |
| 1024 | 极高质量 | 12-20分钟 | 科研级 |

**推荐**：使用 `256`

#### 4. 测量距离设置

**自动模式（推荐）**：
```python
converter = EXRToIESConverter(
    auto_calculate_distance=True,
    measurement_distance=None
)
```
- 根据灯具尺寸自动计算
- 符合IES标准（5倍直径）
- 最小1米，最大20米

**手动模式**：
```python
converter = EXRToIESConverter(
    auto_calculate_distance=False,
    measurement_distance=5.0  # 手动指定5米
)
```
- 适合特殊情况
- 需要确保满足远场条件

---

## 🔄 完整工作流程

### 流程图

```
1. 准备灯具模型
   ↓
2. 定义灯具参数 (LuminaireParameters)
   ↓
3. 创建转换器 (EXRToIESConverter)
   ↓
4. 执行转换 (process_full_workflow)
   ├─ 计算辐射瓦数
   ├─ 设置Blender场景
   ├─ 设置灯光功率
   ├─ 创建测量球体
   ├─ 烘焙EXR
   ├─ 读取EXR数据
   ├─ 转换为IES数据
   └─ 写入IES文件
   ↓
5. 输出 EXR + IES 文件
```

### 详细步骤

#### 步骤1：准备灯具

```python
import bpy

# 方法1：使用现有对象
luminaire = bpy.data.objects['My_Luminaire']

# 方法2：使用当前选中对象
luminaire = bpy.context.active_object

# 方法3：通过名称查找
luminaire = bpy.data.objects.get("Downlight")
if not luminaire:
    print("灯具未找到")
```

**要求**：
- ✅ 灯具应包含完整的几何结构
- ✅ 包括反射器、灯罩、外壳等
- ✅ 至少包含一个Light对象
- ✅ 材质设置正确（反射器=金属，灯罩=半透明等）

#### 步骤2：定义参数

```python
from exr_to_ies_converter import LuminaireParameters

params = LuminaireParameters(
    # 必需参数
    name="LED Downlight",
    lumens=1000.0,
    source_type="led",
    color_temp=4000,
    
    # 推荐参数
    width=0.15,
    length=0.15,
    height=0.10,
    
    # 可选参数
    manufacturer="Custom",
    cri=80,
    beam_angle=60
)
```

#### 步骤3：创建转换器

```python
from exr_to_ies_converter import EXRToIESConverter

converter = EXRToIESConverter(
    resolution='high',          # 4K
    sphere_density='standard',  # 72×36
    cycles_samples=256          # 标准采样
)
```

#### 步骤4：执行转换

```python
success = converter.process_full_workflow(
    luminaire_object=luminaire,
    luminaire_params=params,
    output_exr_path="output/my_light.exr",
    output_ies_path="output/my_light.ies",
    ies_resolution='standard'  # 72×36 IES
)

if success:
    print("✅ 转换成功！")
else:
    print("❌ 转换失败")
```

---

## 💡 实用技巧

### 技巧1：批量生成不同亮度版本

```python
# 只烘焙一次EXR
converter.setup_scene()
radiant_watts, _ = converter.calculate_radiant_watts(params)
converter.set_light_power(luminaire, radiant_watts)
sphere = converter.create_measurement_sphere(luminaire, params)
mat, image = converter.setup_measurement_material(sphere)
converter.bake_light_distribution(sphere, "base.exr")

# 读取一次EXR
exr_data = converter.read_exr_data("base.exr")

# 生成多个IES版本
for lumens in [500, 1000, 1500, 2000]:
    params.lumens = lumens
    candela, h, v = converter.convert_exr_to_ies_data(
        exr_data, lumens, 'standard'
    )
    converter.write_ies_file(
        f"output/light_{lumens}lm.ies",
        params, candela, h, v
    )
```

**优势**：
- ✅ 节省时间（只烘焙一次）
- ✅ 配光形状相同
- ✅ 仅亮度不同

### 技巧2：快速预览配置

```python
# 预览配置：低分辨率+少采样
preview_converter = EXRToIESConverter(
    resolution='low',           # 1K
    sphere_density='preview',   # 36×18
    cycles_samples=64           # 快速
)

# 先预览
preview_converter.process_full_workflow(...)

# 满意后用高质量配置
final_converter = EXRToIESConverter(
    resolution='high',
    sphere_density='standard',
    cycles_samples=256
)
final_converter.process_full_workflow(...)
```

### 技巧3：验证EXR数据

```python
# 读取EXR后验证
exr_data = converter.read_exr_data("output.exr")

print(f"最大辐照度: {exr_data.max():.6f} W/m²")
print(f"平均辐照度: {exr_data.mean():.6f} W/m²")

# 如果最大值异常小或大，检查：
# 1. 灯光功率设置
# 2. 材质反射率
# 3. 测量距离
```

---

## ⚠️ 常见问题

### 问题1：烘焙时间过长

**原因**：
- 采样数过高
- EXR分辨率过高
- CPU渲染

**解决**：
```python
# 启用GPU
scene.cycles.device = 'GPU'

# 或降低采样
converter = EXRToIESConverter(cycles_samples=128)

# 或降低分辨率
converter = EXRToIESConverter(resolution='medium')
```

### 问题2：IES文件在CG软件中太暗/太亮

**原因**：流明值不正确

**解决**：
```python
# 方法1：修改参数重新生成
params.lumens = 1500.0  # 调整流明值
converter.process_full_workflow(...)

# 方法2：直接修改IES文件
# 使用 modify_ies_lumens.py 工具
python modify_ies_lumens.py output.ies -l 1500
```

### 问题3：配光形状不对

**检查清单**：
1. ✅ 灯具模型是否完整（反射器、灯罩）
2. ✅ 材质是否正确（反射器=金属）
3. ✅ 灯光位置是否正确
4. ✅ 测量距离是否合理

### 问题4：极点区域有伪影

**原因**：球体UV在极点收缩

**解决**：
```python
# 使用更高的网格密度
converter = EXRToIESConverter(
    sphere_density='fine'  # 144×72
)
```

---

## 📊 配置推荐表

### 按灯具类型

| 灯具类型 | EXR分辨率 | 球体密度 | 采样数 | 时间 |
|---------|----------|---------|--------|------|
| 筒灯 | high (4K) | standard | 256 | 3-5min |
| 吊灯 | high (4K) | standard | 256 | 3-5min |
| 壁灯 | medium (2K) | standard | 256 | 2-3min |
| 台灯 | medium (2K) | standard | 256 | 2-3min |
| 投光灯 | high (4K) | standard | 256 | 3-5min |
| 汽车灯 | ultra (8K) | fine | 512 | 15-30min |
| 聚光灯 | high (4K) | fine | 256 | 4-6min |

### 按精度要求

| 精度要求 | EXR分辨率 | 球体密度 | 采样数 |
|---------|----------|---------|--------|
| 预览 | low | preview | 64 |
| 一般 | medium | standard | 128 |
| **标准** | **high** | **standard** | **256** |
| 高精度 | high | fine | 512 |
| 科研级 | ultra | fine | 1024 |

---

## 📂 输出文件说明

### EXR文件

**格式**：OpenEXR, 32位浮点, ZIP压缩  
**大小**：30MB - 450MB（取决于分辨率）  
**用途**：
- 存档备份
- 生成不同流明的IES
- 数据分析和可视化

### IES文件

**格式**：IES LM-63-2002 标准文本  
**大小**：10KB - 200KB（取决于分辨率）  
**用途**：
- 在CG软件中使用（Blender/3ds Max/UE/Unity）
- 照明模拟软件
- DIALux, Relux等照明设计软件

---

## 🎓 最佳实践

### ✅ 推荐做法

1. **使用真实流明值**
   - 从产品规格书获取
   - 或从实测数据获取

2. **完整的灯具模型**
   - 包含所有光学组件
   - 真实的材质设置

3. **标准配置起步**
   - 先用标准配置测试
   - 满意后再提高精度

4. **保存EXR文件**
   - 可以后期生成不同版本
   - 无需重新烘焙

5. **验证结果**
   - 在CG软件中测试
   - 对比实际灯具效果

### ❌ 避免的做法

1. ❌ 过度追求精度
   - 8K EXR通常不必要
   - 时间成本高

2. ❌ 忽略灯具遮挡
   - 遮挡是配光的一部分
   - 必须保留

3. ❌ 使用任意流明值
   - 应该使用真实数据
   - 方便验证和调试

4. ❌ 测量距离过小
   - 违反远场条件
   - 结果不准确

---

## 🔗 相关文档

- `exr_to_ies_converter.py` - 主程序
- `example_usage.py` - 使用示例
- `ProductsParametersToRadiantWatts.py` - 辐射瓦特计算
- `modify_ies_lumens.py` - IES流明值修改工具
- `EXR转IES技术方案.md` - 技术原理
- `测量球体网格密度分析.md` - 精度分析
- `测量距离计算方法.md` - 距离计算

---

**版本**: 1.0  
**日期**: 2026-01-21  
**快速开始**: 参见 `example_usage.py`
