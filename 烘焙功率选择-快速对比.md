# 烘焙功率选择 - 快速对比

## 三种方案对比

| 特性 | 方案1: 真实辐射瓦数<br/>⭐⭐⭐⭐⭐ | 方案2: 固定标准值<br/>⭐⭐⭐ | 方案3: 1W功率<br/>⭐⭐ |
|-----|---------------------------|----------------------|------------------|
| **设置功率** | 计算得出<br/>(如 10W) | 固定值<br/>(如 100W) | 始终 1W |
| **EXR物理意义** | ✅ 有 | ❌ 无 | ❌ 无 |
| **可验证性** | ✅ 强 | ❌ 弱 | ❌ 弱 |
| **可调试性** | ✅ 容易 | ⚠️ 困难 | ⚠️ 困难 |
| **工作流程** | 完整清晰 | 需要额外步骤 | 极简但混乱 |
| **IES配光** | ✅ 正确 | ✅ 正确 | ✅ 正确 |
| **学习曲线** | 中等 | 低 | 低 |
| **团队协作** | ✅ 易理解 | ⚠️ 需说明 | ❌ 易混淆 |
| **长期维护** | ✅ 优秀 | ⚠️ 一般 | ❌ 困难 |
| **推荐度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |

---

## 方案1详解（推荐）

### 工作流程

```python
# 步骤1: 计算真实辐射瓦数
radiant_watts = calculate_radiant_watts(
    lumens=1000, 
    source_type="led", 
    color_temp=4000
)
# → 10.0 W

# 步骤2: 设置Blender
light.energy = 10.0

# 步骤3: 烘焙EXR
bake_exr("distribution.exr")
# EXR数值范围: 0.001 - 0.02 W/m²
# ✅ 可以验证是否合理

# 步骤4: 转换IES
convert_to_ies(
    exr="distribution.exr",
    lumens=1000,
    output="luminaire_1000lm.ies"
)

# 步骤5（可选）: 生成其他版本
modify_ies_lumens("luminaire_1000lm.ies", 1500)
# → luminaire_1500lm.ies
```

### 优势示例

```python
# 调试场景：IES在CG软件中太暗

# 方案1：
# 1. 检查EXR最大值：0.002 W/m²
# 2. 理论估算：10W / (4π × 5²) ≈ 0.032 W/m²
# 3. 发现：实际值比理论小15倍
# 4. 排查原因：
#    → 检查材质反射率：发现设置为0.1（错误！）
#    → 改为1.0，重新烘焙
#    → 问题解决！✅

# 方案2或3：
# 1. EXR值无法判断是否合理
# 2. 不知道问题出在哪
# 3. 只能盲目调整参数
# 4. 浪费大量时间 ❌
```

---

## 方案2详解（备选）

### 适用场景

- 快速批量处理
- 不需要验证EXR
- 熟练用户

### 工作流程

```python
# 固定使用100W
STANDARD_POWER = 100.0

# 对所有灯具
for luminaire in all_luminaires:
    light.energy = STANDARD_POWER
    bake_exr(f"{luminaire.name}.exr")
    convert_to_ies(
        exr=f"{luminaire.name}.exr",
        lumens=get_actual_lumens(luminaire),
        output=f"{luminaire.name}.ies"
    )
```

### 注意事项

⚠️ **必须在代码中注释**：
```python
# 注意：使用固定100W功率，EXR数值无物理意义
# 仅用于生成IES配光分布
```

---

## 方案3详解（不推荐）

### 问题

```python
# 问题1：功率-流明脱钩
light.energy = 1.0  # 1W
# 问题：1W对应多少流明？
# LED: 100 lm?
# 白炽灯: 15 lm?
# 完全混乱！

# 问题2：无法验证
# EXR数值 = 1W时的值
# 但实际灯具可能是10W、50W、100W
# 数值差10-100倍，无法判断对错

# 问题3：容易出错
convert_to_ies(
    exr="lamp_1w.exr",  # 1W烘焙的
    lumens=5000,        # 但转换成5000流明
    ...
)
# 这个5000流明对应多少瓦？不清楚！
```

---

## 数学证明

### 归一化原理

```
cd/klm(θ) = [I(θ) / L] × 1000
          = [(E(θ) × d²) / L] × 1000
          = [(k × P × d²) / L] × 1000

其中：
- E(θ): EXR辐照度（与功率P成正比）
- d: 测量距离
- P: 灯光功率
- L: 总流明（与功率P成正比）
- k: 配光系数（仅由灯具几何决定）

关键：
如果 P 和 L 同比例变化：
cd/klm ∝ (k × P) / P = k (常数)

结论：
cd/klm 与功率P无关！
```

### 实例计算

```
场景A: P=10W, L=1000lm
---------------------
EXR(0°) = 0.016 W/m²
I(0°) = 0.016 × 5² = 0.4 W/sr
cd/klm(0°) = (0.4 / 1000) × 1000 = 0.4

场景B: P=100W, L=10000lm (10倍)
--------------------------------
EXR(0°) = 0.16 W/m² (10倍)
I(0°) = 0.16 × 5² = 4.0 W/sr (10倍)
cd/klm(0°) = (4.0 / 10000) × 1000 = 0.4 (相同！)
```

---

## 决策指南

### 你应该选择哪个方案？

```
问题1: 你的项目类型？
├─ 专业项目（商业/科研）
│  → 选方案1 ⭐⭐⭐⭐⭐
│     原因：可验证、可维护
│
├─ 个人项目（学习/实验）
│  → 选方案1 ⭐⭐⭐⭐⭐
│     原因：学习正确流程
│
└─ 快速原型（概念验证）
   → 可选方案2 ⭐⭐⭐
      但记得注释说明！

问题2: 团队规模？
├─ 多人协作
│  → 必选方案1 ⭐⭐⭐⭐⭐
│     原因：他人容易理解
│
└─ 个人独立
   → 推荐方案1 ⭐⭐⭐⭐⭐
      但方案2也可接受

问题3: 需要验证吗？
├─ 需要（质量保证）
│  → 必选方案1 ⭐⭐⭐⭐⭐
│
└─ 不需要（快速出结果）
   → 方案2可接受 ⭐⭐⭐

问题4: EXR多用途？
├─ 是（分析/可视化/对比）
│  → 必选方案1 ⭐⭐⭐⭐⭐
│
└─ 否（仅生成IES）
   → 方案2可接受 ⭐⭐⭐
```

---

## 最终建议

### 🏆 标准答案

**99%的情况下，选择方案1（真实辐射瓦数）**

理由：
1. ✅ 最佳实践
2. ✅ 易于理解
3. ✅ 易于验证
4. ✅ 易于维护
5. ✅ 专业标准

### 💡 例外情况

只有在以下**全部条件**都满足时，才考虑方案2：
- [ ] 非常熟练，完全理解原理
- [ ] 只关心IES配光，不需要验证
- [ ] EXR仅用于生成IES，无其他用途
- [ ] 已在代码中明确注释说明
- [ ] 不涉及团队协作

### ❌ 不推荐

**方案3（1W功率）不推荐用于任何正式项目**

---

## 记忆要点

### 一句话总结

> **虽然数学上任何功率都行，但用真实值让一切更简单、更可靠、更专业。**

### 快速判断

```
需要验证？ → 方案1
需要调试？ → 方案1
多人协作？ → 方案1
专业项目？ → 方案1
个人学习？ → 方案1
快速原型？ → 方案2（勉强）
其他情况？ → 方案1
```

---

**结论：当你不确定时，选方案1就对了！** ✅

---

**文档版本**: 1.0  
**创建日期**: 2026-01-21  
**推荐方案**: 方案1（真实辐射瓦数）⭐⭐⭐⭐⭐
