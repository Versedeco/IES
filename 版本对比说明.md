# EXR to IES Converter - 版本对比

## 📊 两个版本概览

本项目提供**两个版本**的转换工具，适应不同的使用场景。

---

## 🔍 详细对比

| 特性 | Blender完整版 | 独立版 ⭐ |
|-----|-------------|---------|
| **文件名** | `exr_to_ies_converter.py` | `standalone_exr_to_ies.py` |
| **依赖** | Blender 2.93+ | Python + NumPy |
| **输入** | 灯具3D模型 | EXR文件 |
| **输出** | EXR + IES | IES |
| **烘焙** | 自动烘焙EXR | 需要预先生成EXR |
| **运行方式** | Blender脚本 | 命令行/Python脚本 |
| **速度** | 3-30分钟 | <30秒 |
| **灵活性** | 完整工作流 | 仅转换 |
| **适用场景** | Blender用户 | 任何渲染器 |

---

## 🎯 选择建议

### 选择Blender完整版，如果你：

✅ 使用Blender进行灯具设计  
✅ 需要从3D模型生成IES  
✅ 需要完整的自动化工作流  
✅ 需要自动计算测量距离和功率  

**优势**：
- 一键完成所有步骤
- 自动处理复杂参数
- 保留EXR用于后续处理

**劣势**：
- 必须在Blender中运行
- 烘焙耗时较长
- 不能处理其他软件的EXR

### 选择独立版，如果你：

✅ 使用3ds Max / V-Ray / Corona / Arnold等  
✅ 已有现成的EXR文件  
✅ 需要快速批量转换  
✅ 需要集成到自动化流程  

**优势**：
- 不依赖任何3D软件
- 转换速度极快
- 可处理任何来源的EXR
- 易于自动化和批处理

**劣势**：
- 需要手动提供测量距离
- 需要预先生成EXR
- 不包含烘焙功能

---

## 💡 实际应用场景

### 场景1：Blender灯具设计

**使用**: Blender完整版

```python
# 在Blender中
from exr_to_ies_converter import EXRToIESConverter, LuminaireParameters

params = LuminaireParameters(lumens=1000, ...)
converter = EXRToIESConverter()
converter.process_full_workflow(luminaire, params, "out.exr", "out.ies")
```

**流程**: 模型 → 自动烘焙 → EXR → IES

---

### 场景2：3ds Max / V-Ray 工作流

**使用**: 独立版

```bash
# 1. 在3ds Max中渲染带VRayLightMtl的球体为EXR
# 2. 命令行转换
python standalone_exr_to_ies.py vray_light.exr light.ies -l 1500 -d 5.0
```

**流程**: 模型 → 手动渲染 → EXR → (独立版转换) → IES

---

### 场景3：Corona Renderer 工作流

**使用**: 独立版

```bash
# 1. Corona渲染LightMtl球体
# 2. 批量转换
for lumens in 500 1000 1500 2000; do
    python standalone_exr_to_ies.py base.exr light_${lumens}lm.ies -l $lumens -d 5.0
done
```

**流程**: 渲染一次 → 生成多个IES版本

---

### 场景4：已有EXR库

**使用**: 独立版

```bash
# 处理现有的EXR文件集合
python standalone_exr_to_ies.py library/light_001.exr ies/light_001.ies -l 1000 -d 5.0
python standalone_exr_to_ies.py library/light_002.exr ies/light_002.ies -l 1500 -d 5.0
```

**优势**: 快速批量处理

---

### 场景5：混合工作流

**使用**: 两个版本配合

```
步骤1: 在Blender中设计灯具
步骤2: 使用Blender版生成基准EXR
步骤3: 保存EXR到资源库
步骤4: 需要不同版本时，用独立版快速转换
```

**优势**: 结合两者优点

---

## 📋 功能对比详解

### 1. 输入要求

**Blender版**:
```
必需：
- Blender场景文件
- 灯具3D模型（包含反射器、灯罩等）
- Light对象
```

**独立版**:
```
必需：
- EXR文件（equirectangular格式）
- 测量距离值（-d参数）
- 流明值（-l参数）
```

### 2. 输出内容

**Blender版**:
```
输出：
- 高质量EXR文件（32位浮点）
- IES文件
- 详细的计算报告
```

**独立版**:
```
输出：
- IES文件
- 简要的转换报告
```

### 3. 参数设置

**Blender版**:
```python
# 16个灯具参数
params = LuminaireParameters(
    name, manufacturer, catalog_number,
    lumens, source_type, color_temp, cri,
    width, length, height,
    beam_angle, field_angle,
    photometric_type, electrical_watts
)

# 自动计算：
- 测量距离（基于灯具尺寸）
- 辐射瓦特（基于光源类型）
- 球体配置
```

**独立版**:
```bash
# 必需参数
-l LUMENS          # 流明值
-d DISTANCE        # 测量距离（关键！）

# 可选参数
-r RESOLUTION      # IES分辨率
--manufacturer     # 元数据
--name            # 元数据
```

### 4. 性能

**Blender版**:
```
时间：
- 场景设置：10-30秒
- EXR烘焙：3-30分钟（取决于配置）
- IES转换：5-10秒
总计：3-30分钟

内存：
- 峰值：500MB - 2GB（取决于EXR分辨率）
```

**独立版**:
```
时间：
- EXR读取：1-5秒
- IES转换：5-15秒
总计：<30秒

内存：
- 峰值：50MB - 800MB（取决于EXR分辨率）
```

---

## 🎓 推荐工作流程

### 工作流A：纯Blender

```
Blender建模 → Blender版 → EXR + IES
              ↓
              保存EXR备份
```

**适合**: Blender重度用户

---

### 工作流B：纯3ds Max

```
3ds Max建模 → VRay渲染 → EXR → 独立版 → IES
```

**适合**: 3ds Max用户

---

### 工作流C：混合（推荐）

```
任何3D软件建模
    ↓
任何渲染器生成EXR
    ↓
独立版批量转换
    ↓
多个IES版本
```

**适合**: 团队协作、多软件环境

---

### 工作流D：资源库管理

```
各种来源的EXR → 统一资源库
                    ↓
              独立版 → IES库
                    ↓
              按需生成不同版本
```

**适合**: 大型项目、资源管理

---

## 💻 代码示例对比

### Blender版示例

```python
# 在Blender脚本编辑器中
import bpy
from exr_to_ies_converter import EXRToIESConverter, LuminaireParameters

# 选择灯具
luminaire = bpy.context.active_object

# 定义参数（完整）
params = LuminaireParameters(
    name="LED Downlight",
    lumens=1000.0,
    source_type="led",
    color_temp=4000,
    width=0.15,
    length=0.15,
    height=0.10
)

# 创建转换器（可配置）
converter = EXRToIESConverter(
    resolution='high',
    sphere_density='standard',
    cycles_samples=256
)

# 一键完成所有步骤
converter.process_full_workflow(
    luminaire_object=luminaire,
    luminaire_params=params,
    output_exr_path="output/light.exr",
    output_ies_path="output/light.ies"
)
```

### 独立版示例

```bash
# 在命令行中
python standalone_exr_to_ies.py input.exr output.ies -l 1000 -d 5.0
```

或Python脚本：

```python
from standalone_exr_to_ies import StandaloneEXRToIES

converter = StandaloneEXRToIES(measurement_distance=5.0)

converter.convert(
    exr_path="input.exr",
    output_ies_path="output.ies",
    total_lumens=1000.0,
    ies_resolution='standard'
)
```

---

## 📚 文档指南

### Blender版文档

- `使用指南.md` - 完整使用说明
- `example_usage.py` - 代码示例
- `EXR转IES技术方案.md` - 技术原理

### 独立版文档

- `独立版使用指南.md` - 完整使用说明
- `standalone_exr_to_ies.py` - 代码即文档（详细注释）

### 通用文档

- `README.md` - 项目概览
- `测量距离计算方法.md` - 距离选择
- `灯光功率与IES强度的关系.md` - 原理说明

---

## 🚀 快速决策

### 30秒决策树

```
你使用Blender吗？
├─ 是 → 使用Blender完整版
│
└─ 否 → 你有现成的EXR吗？
        ├─ 是 → 使用独立版 ⭐
        └─ 否 → 先用任何渲染器生成EXR
                → 然后使用独立版 ⭐
```

---

## ✅ 总结

### Blender完整版

**核心优势**: 自动化、完整工作流  
**最佳场景**: Blender灯具设计  
**关键文件**: `exr_to_ies_converter.py`  
**文档**: `使用指南.md`  

### 独立版 ⭐

**核心优势**: 通用性、快速、灵活  
**最佳场景**: 处理现有EXR、多软件环境  
**关键文件**: `standalone_exr_to_ies.py`  
**文档**: `独立版使用指南.md`  

---

**建议**: 
- 大多数用户从**独立版**开始 ⭐
- Blender重度用户使用**完整版**
- 两者可以配合使用

---

**最后更新**: 2026-01-21  
**版本**: 1.0
